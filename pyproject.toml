[project]
name = "meeting-summary"
version = "0.1.0"
description = "Meeting summary from transcript"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "python-dotenv>=1.0.0",
    "openai>=1.101.0",
    "pyyaml>=6.0.0",
]
