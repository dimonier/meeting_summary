[project]
name = "meeting-summary"
version = "0.1.0"
description = "Meeting summary from transcript"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "dotenv>=0.9.9",
    "openai>=1.101.0",
]
